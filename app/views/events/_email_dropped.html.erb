Email dropped:
<%= link_to event.campaign_name, campaign_url(event.campaign_id) %>
(<i><%= event.subject %></i>)
for
<%= link_to event.email_address, customer_url(event.customer_id) %>
(<%= link_to time_ago(event), event %>)
